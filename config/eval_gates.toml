[validation]
out_of_sample_profit_factor_floor = 1.05
expectancy_floor = 0.0
minimum_test_trade_count = 100
drawdown_review_trigger_pct = 12.0
stress_spread_multiplier = 1.25
stress_profit_factor_floor = 1.0
stress_slippage_pips = 0.25
stress_fill_delay_ms = 500
walk_forward_windows = 3
walk_forward_mode = "anchored_time_windows"
walk_forward_profit_factor_floor = 0.90
walk_forward_min_trades_per_window = 10
walk_forward_min_window_days = 7
max_relative_drawdown_degradation_pct = 15.0
deflated_sharpe_floor = 0.0
cscv_partition_count = 8
pbo_threshold = 0.35
white_reality_check_bootstrap_samples = 250
white_reality_check_block_size = 5
white_reality_check_pvalue_threshold = 0.10
white_reality_check_random_seed = 1729
forward_min_trading_days = 10
forward_min_trade_count = 25
forward_profit_factor_floor = 1.0
forward_expectancy_floor = 0.0
forward_expectancy_degradation_limit_pct = 50.0
parity_timestamp_tolerance_seconds = 90
parity_close_timing_tolerance_seconds = 120
parity_price_tolerance_pips = 0.30
parity_fill_tolerance_pips = 0.50
parity_min_match_rate = 0.80
parity_max_unmatched_expected_rate = 0.20
parity_max_unmatched_actual_rate = 0.20
parity_min_closed_trades = 10

[campaign]
default_family = "scalping"
default_max_iterations = 2
default_max_new_candidates = 8
default_trial_cap_per_family = 50
stop_on_review_eligible_provisional = true
max_mt5_parity_retries_per_candidate = 2
max_shadow_forward_retries_per_candidate = 1
max_total_operational_runs_per_campaign = 4

[ml_hardening]
label_randomization_accuracy_ceiling = 0.55
adversarial_auc_threshold = 0.55
purged_cv_embargo_minimum_bars = 10
feature_importance_top3_floor = 0.40
model_persistence_format = "joblib"

[eva_optimizer]
default_population_size = 30
default_generations = 80
fitness_pbo_penalty_weight = 0.3
stop_loss_pips_bounds = [5.0, 50.0]
take_profit_pips_bounds = [5.0, 100.0]
signal_threshold_bounds = [0.3, 0.9]
holding_bars_bounds = [5, 120]

[regime_classifier]
n_components_range = [3, 5]
regime_stability_min_window_agreement = 0.60

[mt5_alignment]
feature_alignment_auc_threshold = 0.60

[signal_filter]
model_type = "xgboost"
max_leaves = 500

[gp_rules]
population_size = 150
generations = 75
max_tree_depth = 7
parsimony_coefficient = 0.01
crossover_probability = 0.7
mutation_probability = 0.2
tournament_size = 5
min_signals_for_recommendation = 20
min_pf_for_recommendation = 1.0

# ---------------------------------------------------------------------------
# Phase 1.6.0 — Variance Pilot calibration outputs.
# Populated by scripts/run_ml_variance_pilot.py after the pilot runs.
# Do NOT hand-edit these values; downstream phase gates read them and any
# hand-tuning would defeat the "measure noise before locking thresholds"
# discipline that motivates the pilot.
# ---------------------------------------------------------------------------
[ml_variance_pilot]
# Locked from PILOT-1.6.0-20260420 (10 seeds × 3 candidates: AF-CAND-0007,
# AF-CAND-0278, AF-CAND-0290) on data/normalized/research/eur_usd_m1.parquet.
# σ_PF was measured at 0.0083 — well under the 0.08 hard-exit gate, so the
# pilot proceeds without a data-collection sub-phase.
sigma_pf = 0.0083
mde_pf = 0.0083
effect_size_floor_pf = 0.0083
required_n_candidates = 11
n_seeds = 10
n_candidates = 3
pilot_report_path = "Goblin/reports/ml/p1_6_0_variance_pilot.json"
pilot_id = "PILOT-1.6.0-20260420"

# ---------------------------------------------------------------------------
# Phase 1.6 — Baseline Evidence Foundation locked artifacts.
# Locked at run time; tools/derive_mde.py refuses to run on a mismatched SHA
# (R4-3 Constraint 4 — fail loudly on input drift, do not silently re-derive).
# ---------------------------------------------------------------------------
[ml_baseline_comparison]
dataset_sha = "7875ba5af620476aaba80cdec0c86680b343f70a02bf19eb40695517378ed8f1"
dataset_path = "data/normalized/research/eur_usd_m1.parquet"
report_path = "Goblin/reports/ml/p1_6_baseline_comparison.json"
report_sha = ""  # populated by EX-1 first run; subsequent runs assert match
run_id = "BASELINE-1.6-20260420"
n_candidates = 11
n_folds = 3

# ---------------------------------------------------------------------------
# Phase 2.0 — Re-Gate calibration (R4-3 outputs; EX-1 phase).
# Populated by tools/derive_mde.py from the locked 1.6 baseline report.
# Do NOT hand-edit. Conservative-tier-on-upper-bound rule applies (I1):
# if mde_pf_at_upper_ci_bound crosses an I1 tier boundary, pre-commit to
# the more conservative tier in DEC-ML-2.0-TARGET.
# ---------------------------------------------------------------------------
[ml_p2]
sigma_cross_point = 0.0211004853
sigma_cross_ci_low = 0.0136548471
sigma_cross_ci_high = 0.0248926354
mde_pf_point = 0.0426550036
mde_pf_at_upper_ci_bound = 0.0503209020
n_survivors = 6
alpha = 0.01
power = 0.80
bootstrap_n_resamples = 10000
bootstrap_rng_seed = 20260420
derivation_manifest_path = "Goblin/reports/ml/p2_0_mde_derivation_manifest.json"
derivation_manifest_sha = "1823043a7940bc4fceec5b72badad1be4dbeafc61c22ca7d82043283775f6bf1"


# ---------------------------------------------------------------------------
# Phase 2.0 - Frozen regime split thresholds (EX-6).
# Computed once from the in-sample (non-holdout, chronologically-earliest 80%)
# portion of dataset_sha pinned above. Replaces the per-candidate-recomputed
# medians in src/agentic_forex/ml/baseline_runner.py:assign_regimes for all
# Phase 2 work. Frozen so cross-candidate regime-fragility comparisons are
# apples-to-apples and not perturbed by per-candidate label-NaN drops.
# Modifications require @joseph-romeo CODEOWNERS review AND a new EX-6.x
# decision log entry. Test tests/test_regime_freeze.py asserts immutability.
# ---------------------------------------------------------------------------
[ml_regime]
abs_momentum_12_median = 1.9000000000
volatility_20_median = 0.0000741639
n_in_sample_rows_used = 155775
in_sample_holdout_first_index = 155775
dataset_sha256_at_freeze = "7875ba5af620476aaba80cdec0c86680b343f70a02bf19eb40695517378ed8f1"
frozen_at_utc = "2026-04-21T00:00:00Z"
frozen_in_phase = "ML-P2-EX-6"